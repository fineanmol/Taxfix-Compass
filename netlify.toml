[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Vite SPA: serve index.html for all routes so deep links and refresh work.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
